;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-advanced-reader.ss" "lang")((modname fi-beginner) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #t #t none #f () #f)))
(require "provide_helper.rkt")
    
(provide define    
         define-struct
         cond
         if
         and
         or
         check-expect
         check-within
         require
         + - * /
         < > <= >= =
         sqr
         sqrt
         abs
         expt
         max
         min
         random
         add1
         sub1
         modulo
         pi
         floor
         ceiling
         round
         sin
         cos
         tan
         asin
         acos
         atan
         number?
         integer?
         odd?
         even?
         zero?
         positive?
         negative?
         number->string
         current-seconds
         boolean?
         boolean=?
         false?
         not
         string-append
         string-length
         substring
         string=?
         string->number
         exact->inexact
         string?
         cons
         list
         append
         remove
         first
         range
         rest
         length
         list-ref
         member?
         cons?
         empty?
         image?
         image=?
         make-posn
         posn?
         posn-x
         posn-y
         equal?
         eqv?
         true
         false
         empty
         local  ;; ISL 
         letrec ;; ISL 
         let    ;; ISL
         begin  ;; ASL
         )
         
         
         